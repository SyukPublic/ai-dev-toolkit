# Fixture Plugin

A plugin used only by the site indexer tests. It demonstrates the full
component layout: a skill, an agent, a command, a hook, and an MCP server.

## What it executes

- One PostToolUse hook (`scripts/check.sh`)
- One MCP server (`sample-server`)
