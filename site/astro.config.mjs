import { defineConfig } from 'astro/config';

// Project site on GitHub Pages — everything is served under the repo subpath.
export default defineConfig({
  site: 'https://syukpublic.github.io',
  base: '/ai-dev-toolkit',
});
